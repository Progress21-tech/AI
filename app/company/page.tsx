import { requireUser } from '@/lib/auth/server';
import { createServerSupabaseClient } from '@/lib/supabase/server';
export default async function CompanyPage() { await requireUser(); const supabase = await createServerSupabaseClient(); const { data: companies = [] } = supabase ? await supabase.from('companies').select('id,name,industry,website,size,created_at').order('created_at', { ascending: false }) : { data: [] }; return <main className="min-h-screen bg-white p-6 text-black"><div className="mx-auto max-w-4xl"><h1 className="mb-6 text-3xl font-bold">Company details</h1><div className="space-y-3">{companies.map((company) => <article key={company.id} className="rounded-2xl border border-borderDark p-5"><h2 className="font-semibold">{company.name}</h2><p className="text-sm text-subtle">{company.industry || 'Industry not captured'} · {company.size || 'Size not captured'}</p></article>)}</div></div></main>; }
