import Link from 'next/link';
import { requireAdmin } from '@/lib/auth/server';
import { createServerSupabaseClient } from '@/lib/supabase/server';
export default async function AdminInterviewsPage() { await requireAdmin(); const supabase = await createServerSupabaseClient(); const { data: interviews = [] } = supabase ? await supabase.from('interviews').select('id,status,started_at,companies(name)').order('started_at', { ascending: false }) : { data: [] }; return <main className="min-h-screen bg-white p-6 text-black"><div className="mx-auto max-w-5xl"><h1 className="mb-6 text-3xl font-bold">Interviews</h1><div className="space-y-3">{interviews.map((interview: { id: string; status: string; started_at: string; companies: { name: string } | null }) => <Link key={interview.id} href={`/admin/interviews/${interview.id}`} className="block rounded-2xl border border-borderDark p-5 hover:bg-surface"><strong>{interview.companies?.name ?? 'Unknown company'}</strong><p className="text-sm text-subtle">{interview.status} · {new Date(interview.started_at).toLocaleString()}</p></Link>)}</div></div></main>; }
